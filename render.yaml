services:
  - type: web
    name: sen-cam-cong
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
    healthCheckPath: /

databases:
  - name: sen-cam-cong-db
    plan: starter
    databaseName: sen_cam_cong
    user: sen_cam_cong_user

